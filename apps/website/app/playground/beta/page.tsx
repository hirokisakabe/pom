import { BetaAppLayout } from "@/playground/beta/client/BetaAppLayout";

export const metadata = {
  title: "Beta Playground — pom",
  description:
    "Visually edit pom XML slide structure with drag-and-drop AST editing",
};

export default function BetaPlaygroundPage() {
  return <BetaAppLayout />;
}

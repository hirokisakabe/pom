---
"@hirokisakabe/pom-editor": patch
---

許可されない DnD 操作（異なる親への移動）に視覚的フィードバックを追加。ドラッグ中、不正な drop target にカーソルが乗ったとき該当行を赤くハイライトし、カーソルを `not-allowed` に切り替えるようにした。これまで silent に却下されていた状態を解消し、「ここには落とせない」ことが UI から判別できる。

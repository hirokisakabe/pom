---
"@hirokisakabe/pom": minor
"@hirokisakabe/pom-jsx": minor
---

Text / Ul・Ol の Li / Shape / Table cell に `subscript` / `superscript` 属性を追加しました。インラインタグ `<Sub>` / `<Sup>` も追加し、`<Text>H<Sub>2</Sub>O</Text>` のように runs 単位で部分指定できます。`pom-jsx` でも対応する `subscript` / `superscript` props と `<Sub>` / `<Sup>` コンポーネントを追加しました。

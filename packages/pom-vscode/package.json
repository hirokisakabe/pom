{
  "name": "pom-vscode",
  "private": true,
  "displayName": "pom",
  "description": "Live preview for pom-md / pom XML presentations",
  "version": "0.5.0",
  "publisher": "hirokisakabe",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hirokisakabe/pom.git",
    "directory": "packages/pom-vscode"
  },
  "engines": {
    "node": ">=22",
    "vscode": "^1.101.0"
  },
  "icon": "icon.png",
  "categories": [
    "Visualization"
  ],
  "keywords": [
    "presentation",
    "powerpoint",
    "pptx",
    "markdown",
    "preview"
  ],
  "galleryBanner": {
    "color": "#7C3AED",
    "theme": "dark"
  },
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pom.openPreview",
        "title": "pom: Open Preview",
        "icon": {
          "dark": "icons/preview-dark.svg",
          "light": "icons/preview-light.svg"
        }
      },
      {
        "command": "pom.exportPptx",
        "title": "pom: Export PPTX"
      }
    ],
    "configuration": {
      "title": "pom",
      "properties": {
        "pom.preview.defaultZoom": {
          "type": "string",
          "enum": [
            "fit",
            "50",
            "75",
            "100",
            "150"
          ],
          "enumItemLabels": [
            "Fit to Width",
            "50%",
            "75%",
            "100%",
            "150%"
          ],
          "default": "fit",
          "description": "Default zoom level for the pom preview panel."
        }
      }
    },
    "menus": {
      "editor/title": [
        {
          "when": "resourceFilename =~ /\\.pom\\.(md|xml)$/",
          "command": "pom.openPreview",
          "group": "navigation"
        },
        {
          "when": "resourceFilename =~ /\\.pom\\.(md|xml)$/",
          "command": "pom.exportPptx",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "lint": "eslint",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "knip": "knip",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:showcase": "vitest run --config vitest.showcase.config.ts",
    "test:integration": "node esbuild.mjs --test && vscode-test",
    "test:vsix": "node esbuild.mjs --test && vsce package --no-dependencies && node run-vsix-test.mjs",
    "test:vsix:clean": "rm -f *.vsix"
  },
  "devDependencies": {
    "@hirokisakabe/pom": "workspace:*",
    "@hirokisakabe/pom-md": "workspace:*",
    "@types/mocha": "^10.0.10",
    "@types/node": "^26",
    "@types/vscode": "~1.101.0",
    "@vitest/coverage-v8": "^4.1.10",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "mocha": "^11.8.0",
    "pptx-glimpse": "^3.2.8",
    "sharp": "^0.35.3"
  }
}
